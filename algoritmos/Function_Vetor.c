#include <stdlib.h>
#include <stdio.h>

int main(){

    // Passando um array "Por Referência"

    void imprime(int *m,i int n);
    void imprime(int m[], int n);
    void imprime (int m[5], int n);


 system("pause");
 return 0;
}
